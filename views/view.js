(function () {
  'use strict';

  var app = window.app;
  var Backbone = window.Backbone;

  // TODO remove, unless we demonstrate the benefit of having a custom base class
  app.View = Backbone.View.extend({
  });
})();
